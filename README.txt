TODOS
*********************



Usage:
----------------------
- In the field group "Receiver" group_message_receiver we can reference the following that the user can be referenced by

Users	field_message_user_ref	Entity Reference
Groups	field_node_groups_ref	Entity Reference

- We can use these fields to check if the user belongs to them before providing access to the message

